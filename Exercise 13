def area_sum(rectangles):
    combined_areas = 0
    for rectangle in rectangles:
        height = rectangle["height"]
        width = rectangle["width"]
        combined_areas += (height * width)

    return combined_areas
